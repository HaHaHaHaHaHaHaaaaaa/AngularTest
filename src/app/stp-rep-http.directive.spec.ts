import { StpRepHttpDirective } from './stp-rep-http.directive';

describe('StpRepHttpDirective', () => {
  it('should create an instance', () => {
    const directive = new StpRepHttpDirective();
    expect(directive).toBeTruthy();
  });
});
