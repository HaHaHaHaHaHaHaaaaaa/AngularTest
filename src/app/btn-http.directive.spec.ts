import { BtnHttpDirective } from './btn-http.directive';

describe('BtnHttpDirective', () => {
  it('should create an instance', () => {
    const directive = new BtnHttpDirective();
    expect(directive).toBeTruthy();
  });
});
